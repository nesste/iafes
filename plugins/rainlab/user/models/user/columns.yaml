# ===================================
#  Column Definitions
# ===================================

columns:
    name:
        label: Name
        searchable: yes

    university:
        label: university
        relation: university
        select: @name
        searchable: yes

    country:
        label: Country
        relation: country
        select: @name
        searchable: yes
    state:
        label: State
        relation: state
        select: @name
        searchable: yes
    email:
        label: Email
        searchable: yes
    created_at:
        label: Registered
